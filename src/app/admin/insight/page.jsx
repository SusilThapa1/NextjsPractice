import { Button } from "@mui/material";
import React from "react";

const page = () => {
  return (
    <div>
      <Button variant="outlined" color="success">
        Insights page
      </Button>
    </div>
  );
};

export default page;
