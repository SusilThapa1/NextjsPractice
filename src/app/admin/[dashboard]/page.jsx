"use client";

import { Button } from "@mui/material";

const page = () => {
  return (
    <div>
      <Button variant="outlined" color="success">
        Admin dashboard
      </Button>
    </div>
  );
};

export default page;
